PORT=5000
DATABASE_URL=file:../database/test.db

# Usually I create a .env.example with empty values and add this .env file in the .gitignore for security matters, but in this project this won't be necessary.